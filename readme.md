About the language

prodedural language that allows you to define functions, use conditionals, math operations etc. 

supports if/then/else construct, for loop, user defnied operators, JIT compilation with cli

supports only 64-bit floating point, no need for declaring variables all of them are implicitly set to 64-bit floating point type

is allowed to call standard library functions
